# Copy to .env for local development. Real values live in Render's environment settings.

# SQLAlchemy database URL (Supabase Postgres).
# Local example points at a local Postgres; production uses the Supabase connection string.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/job_agent
